info:
  name: GET /next_games no params
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:4000/api/next_games
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
