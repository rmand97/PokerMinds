info:
  name: GET /next_games
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/api/next_games?player_id=stine&suite_id=123
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: player_id
      value: stine
      type: query
    - name: suite_id
      value: "123"
      type: query
  body:
    type: json
    data: |-
      {
        "player_id": "stine",
        "suite_id": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
