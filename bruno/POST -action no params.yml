info:
  name: POST /action no params
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4000/api/action
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
